name = "omix-cron"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Runs every 5 minutes
[triggers]
crons = ["*/5 * * * *"]

# Same D1 database as omix-api
[[d1_databases]]
binding = "DB"
database_name = "omix-db"
database_id = "REPLACE_WITH_D1_DATABASE_ID"
migrations_dir = "../migrations"
