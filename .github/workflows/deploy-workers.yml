name: Deploy Cloudflare Workers

on:
  push:
    branches: [master]
    paths:
      - "workers/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/deploy-workers.yml"
  workflow_dispatch:

concurrency:
  group: workers-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck workers
        run: npm run workers:typecheck

      - name: Verify required secrets are set
        shell: bash
        run: |
          set -e
          for s in CLOUDFLARE_API_TOKEN CLOUDFLARE_ACCOUNT_ID ABLY_API_KEY GITHUB_CLIENT_ID GITHUB_CLIENT_SECRET RESEND_API_KEY VAPID_PRIVATE_KEY; do
            eval "value=\${$s}"
            [ -n "$value" ] || { echo "::error::Required secret $s is not set"; exit 1; }
          done
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          ABLY_API_KEY: ${{ secrets.ABLY_API_KEY }}
          GITHUB_CLIENT_ID: ${{ secrets.GITHUB_CLIENT_ID }}
          GITHUB_CLIENT_SECRET: ${{ secrets.GITHUB_CLIENT_SECRET }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}

      # Ensure the KV namespace exists (errors if already present — that's fine).
      # The namespace id itself is committed in workers/omix-uploads/wrangler.toml.
      - name: Ensure KV namespace
        run: npx wrangler kv namespace create omix-assets || true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # Apply any pending D1 migrations (schema lives in workers/migrations/).
      - name: Apply D1 migrations
        run: npx wrangler d1 migrations apply omix-db --config workers/omix-chat/wrangler.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # Domain services first (they must exist before the gateway can bind),
      # gateway last, then the scheduled worker.
      - name: Deploy domain services
        run: |
          for w in omix-auth omix-chat omix-social omix-servers omix-notifications omix-uploads; do
            npx wrangler deploy --config workers/$w/wrangler.toml || exit 1
          done
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy omix-gateway
        run: npx wrangler deploy --config workers/omix-gateway/wrangler.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy omix-cron
        run: npx wrangler deploy --config workers/omix-cron/wrangler.toml
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Set omix-auth secrets
        run: |
          echo "${{ secrets.GITHUB_CLIENT_ID }}" | npx wrangler secret put GITHUB_CLIENT_ID --config workers/omix-auth/wrangler.toml
          echo "${{ secrets.GITHUB_CLIENT_SECRET }}" | npx wrangler secret put GITHUB_CLIENT_SECRET --config workers/omix-auth/wrangler.toml
          echo "${{ secrets.RESEND_API_KEY }}" | npx wrangler secret put RESEND_API_KEY --config workers/omix-auth/wrangler.toml
          if [ -n "${{ secrets.GITHUB_WEBHOOK_SECRET }}" ]; then
            echo "${{ secrets.GITHUB_WEBHOOK_SECRET }}" | npx wrangler secret put GITHUB_WEBHOOK_SECRET --config workers/omix-auth/wrangler.toml
          fi
          if [ -n "${{ secrets.EMAIL_FROM }}" ]; then
            echo "${{ secrets.EMAIL_FROM }}" | npx wrangler secret put EMAIL_FROM --config workers/omix-auth/wrangler.toml
          fi
          if [ -n "${{ secrets.APP_ORIGIN }}" ]; then
            echo "${{ secrets.APP_ORIGIN }}" | npx wrangler secret put APP_ORIGIN --config workers/omix-auth/wrangler.toml
          fi
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Set omix-gateway secrets
        run: |
          echo "${{ secrets.ABLY_API_KEY }}" | npx wrangler secret put ABLY_API_KEY --config workers/omix-gateway/wrangler.toml
          echo "${{ secrets.VAPID_PRIVATE_KEY }}" | npx wrangler secret put VAPID_PRIVATE_KEY --config workers/omix-gateway/wrangler.toml
          if [ -n "${{ secrets.VAPID_SUBJECT }}" ]; then
            echo "${{ secrets.VAPID_SUBJECT }}" | npx wrangler secret put VAPID_SUBJECT --config workers/omix-gateway/wrangler.toml
          fi
          if [ -n "${{ secrets.APP_ORIGIN }}" ]; then
            echo "${{ secrets.APP_ORIGIN }}" | npx wrangler secret put APP_ORIGIN --config workers/omix-gateway/wrangler.toml
          fi
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Set omix-notifications and omix-cron secrets
        run: |
          echo "${{ secrets.VAPID_PRIVATE_KEY }}" | npx wrangler secret put VAPID_PRIVATE_KEY --config workers/omix-notifications/wrangler.toml
          echo "${{ secrets.VAPID_PRIVATE_KEY }}" | npx wrangler secret put VAPID_PRIVATE_KEY --config workers/omix-cron/wrangler.toml
          if [ -n "${{ secrets.VAPID_SUBJECT }}" ]; then
            echo "${{ secrets.VAPID_SUBJECT }}" | npx wrangler secret put VAPID_SUBJECT --config workers/omix-notifications/wrangler.toml
            echo "${{ secrets.VAPID_SUBJECT }}" | npx wrangler secret put VAPID_SUBJECT --config workers/omix-cron/wrangler.toml
          fi
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
